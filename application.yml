eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/
management:
  context-path: /manage
  security:
    enabled: false
spring:
  boot:
    admin:
      url: http://springBootAdmin:8081
health:
  config:
    enabled: false